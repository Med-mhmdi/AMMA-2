apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: amma-gateway-route
  namespace: amma
spec:
  hosts:
    - "*"
  gateways:
    - amma-istio-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: amma-gateway.amma.svc.cluster.local
            port:
              number: 8000
      retries:
        attempts: 3
        perTryTimeout: 2s
        retryOn: gateway-error,connect-failure,refused-stream,5xx
      timeout: 15s
