apiVersion: v1
kind: Service
metadata:
  name: amma-expense-service
  labels:
    app: amma-expense-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: amma-expense-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
