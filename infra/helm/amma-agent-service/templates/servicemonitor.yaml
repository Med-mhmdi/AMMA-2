apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: amma-agent-service
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: amma-agent-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
