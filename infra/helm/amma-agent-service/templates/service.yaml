apiVersion: v1
kind: Service
metadata:
  name: amma-agent-service
  labels:
    app: amma-agent-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: amma-agent-service
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
