apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: amma-analytics-service
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: amma-analytics-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
