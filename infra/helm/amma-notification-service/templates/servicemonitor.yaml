apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: amma-notification-service
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: amma-notification-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
